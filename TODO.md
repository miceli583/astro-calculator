# TODO

## Critical (blocks production)

- [x] Vercel: confirm calculator endpoints return JSON on production — **verified 2026-07-08**: all 15 endpoints swept on astro-calculator-design.vercel.app; every one returns `application/json` (incl. sweph-powered natal/transits/HD). The `serverExternalPackages` + `outputFileTracingIncludes: "**/*"` fix in next.config.ts resolved it. Evidence in GitHub issue #1 (closed).

## Bugs (broken functionality)

- [x] **Polar charts returned Porphyry cusps under the requested system's name.** — carded `t_9a1538d0`, **fixed 2026-08-13**. `calcHouses`/`calcHousesFromArmc` read `result.data` and discarded `result.flag`; inside the polar circles sweph sets `flag = −1` for Placidus and Koch and substitutes Porphyry cusps (byte-identical, measured). Now gated on the flag, and every chart and composite response carries **`houses.system`** — the system that actually produced the cusps — plus **`houses.requestedSystem`**, present only on a substitution. The warning says what happened instead of calling exact Porphyry cusps "unreliable placidus" ones. The 66.5° constant was not corrected but **removed from the decision**: the true boundary tracks the obliquity (66.532697° in 1800, 66.562323° at J2000, 66.577351° in ≈2333), so no constant can be right and the flag is the only correct source. The suite binary-searches the boundary at three epochs to pin exactly that. (F5)
- [x] **A degenerate polar wheel put every body in house 1.** — carded `t_9a1538d0` with F5, **fixed 2026-08-13**. Above the polar circle, over a window of sidereal time that widens with latitude (0/360 below the circle → 122/360 at Longyearbyen → 176/360 at 89°), Regiomontanus and Campanus cusps collapse and the first forward arc goes negative; `houseFor` read a −0.03° house as a 359.97° one that swallowed the zodiac. The root cause is one step past that: inside those windows the whole wheel runs **retrograde**. Measured across a full sidereal day at 7 latitudes × 7 systems, all 4186 degenerate instants sum to 360×11 forward and to exactly 360 backward — the cusps are exact, only our direction assumption was wrong. `houseSpans` now measures which direction closes the circle rather than assuming forward, and a reversed wheel is announced in `warnings`. Two plausible fixes were rejected with measurements: normalising arcs into [0,360) *is* the bug, and flipping the largest arc by −360° leaves two 270.065° houses overlapping by 180.13° at 66.6°N. Correction to the original write-up: the Longyearbyen Moon and Jupiter are in house **9**, not 3 — "house 3" came from forward-reading the wide arc. `houseFor` also existed twice (`astrology.ts` and `overlay.ts`), so this was one defect in two places; now consolidated. `tests/polar-houses.test.ts` (20 tests) carries the ARMC sweep; write-up in `docs/chart-conventions.md` §6.2 and §7. (F6)
- [x] **Sect (day/night) was read off the Sun's house number, so the Part of Fortune depended on the requested house system.** Fixed — sect now comes from the horizon directly (`isAboveHorizon`: `(ASC − Sun) mod 360 < 180`), shared by the natal and composite calculators, and is invariant across all seven supported house systems. Measured before the fix (Kolkata 1972): Placidus said day, whole-sign said night, and the Part of Fortune moved **135°**. Convention promoted to `docs/aspect-conventions.md` §4.2. (F7)
- [x] **The Part of Fortune was fabricated when the Sun or Moon was absent.** Fixed — the field is now OMITTED entirely (key absent, not null) when the `planets` subset excludes either luminary, on natal, composite, solar-return and planetary-return, and drops out of the transit/synastry overlay point set with it. It previously returned the Ascendant's longitude with `isDayBirth: false`, identical to the last digit and indistinguishable from a real PoF conjunct the ASC. Contract stated in `docs/aspect-conventions.md` §4.2 and the OpenAPI response descriptions. (F8)
- [x] **`/api/v1/astrology/transits` used natal orbs while `/api/v1/transit/natal` used transit orbs.** Fixed — both orb tables moved to `src/lib/constants/orbs.ts` (the import cycle `astrology.ts` ← `overlay.ts` was the structural cause: the transit table was simply out of reach) and `calculateTransits` now reads `DEFAULT_TRANSIT_ORBS`. Measured (Diana, 2026-08-12, planet-to-planet): **54 hits → 18**, matching `/api/v1/transit/natal` exactly; a regression test asserts set equality both ways with per-hit orbs to 9 dp. Convention pinned in `docs/aspect-conventions.md` §1.3: the table is a property of the question, not of the URL. **Two corrections to the original filing**: the endpoint was `/api/v1/astrology/transits`, not `/api/v1/transit` (which computes no transit-to-natal aspects at all), and the "ignores the caller's `orbs` override" claim was false — `transitInputSchema` has no `orbs` field. §8 (F9).
- [x] **`/api/v1/astrology/transits` hardcoded `applying: false` on every aspect.** Fixed — direction now computed from relative motion by a shared `aspectMotion` helper, and the representation is three-valued: `motion` is `"applying" | "separating" | "stationary"`, and `applying` is **omitted** when stationary or indeterminate rather than defaulting to a claim. Measured: **0 of 54 → 10 applying / 8 separating**, matching the overlay path. Stationary tolerance 1e-4 °/day, chosen from a measurement over 1304 aspecting pairs (a 0.01 tolerance would call moving outer bodies stationary). Convention in `docs/aspect-conventions.md` §1.5. (F10)
- [x] **`/api/v1/astrology/transits` accepts no `orbs` override** where `/api/v1/transit/natal` and `/api/v1/synastry` both do. — carded `t_a05bb333`, **fixed 2026-08-13** (PR #22 → dev, PR #23 → main, live on prod). The card's premise was verified before being built on, and held in full. `transitInputSchema` gained `orbs`; `calculateTransits` merges it over `DEFAULT_TRANSIT_ORBS`. Shape matched to the two parity references rather than invented: `/transit/natal` and `/synastry` agree with each other (both `orbs?: OverlayOptions["orbs"]`, both merged in `computeOverlay`), differing only in which default table they merge over, so there was nothing to adjudicate. `calculateTransits` doesn't route through `computeOverlay`, so it merges explicitly the way `calculateSynastry` does. Contract is **merge, not replace** — a partial override leaves the other aspects on their defaults. The acceptance test asserts the override *changes the hit count* in both directions plus a superset check that widening never drops a hit; a test that only proves the field is accepted would prove nothing. Measured on prod (Diana / 2026-08-12): default 18, all-9° 86, all-0.5° 2, `{conjunction: 9}` 25 with only conjunctions moving (3→10); orb 16 and −1 both `422`. Two latent problems fixed on the way: `orbOverrideSchema` was declared *below* `transitInputSchema` and referencing it would have been a module-load **TDZ error**, not a hoisted forward reference (now declared above first use, with a comment saying why); and the two transit paths iterate the aspect table in different orders (`ASPECT_DEFS` vs `Object.keys(ASPECT_ANGLES)`) and both `break` on first match, which can only diverge if two aspect windows overlap — unreachable because the schema's 15° cap is exactly half the 30° minimum gap between exact aspect angles, a property now pinned by a test instead of left to coincidence. 1355 → 1359 tests.
- [x] **Every chart carries a tautological node–node opposition at orb 0.00.** Fixed — the `true_node`/`south_node` pair is excluded in `computeAspects`; the South Node keeps every other aspect and no other pair is suppressed. Convention promoted to `docs/aspect-conventions.md` §1.7. (F11)
- [x] **Every birth date before 1800 returned a 500.** — carded `t_d0498a77`, **fixed 2026-08-13**. `calcPlanet` threw on any non-empty `out.error`, but sweph uses that field for *warnings* too and signals real failure via `flag < 0`. Now gated on the flag, with the ephemeris bit decoded, so every response that returns positions carries **`ephemeris`** (`"swiss" | "moshier" | "jpl" | "mixed"`) naming the source that actually answered. `sepl_12.se1` deliberately **not** shipped (HM's call: ~10 MB of permanent weight for a range no caller has asked for; a labelled fallback is not a downgrade in disguise). Verified through the route: 1799 and 1750 return 200 with `ephemeris: "moshier"`, 1950 with `"swiss"`. The two accuracy-suite exclusions that *asserted* the bug went red on the fix and were replaced with positive contracts; those charts are back in the membership test. (F1) — Chiron pre-1800 was the same card: sweph genuinely refuses it (`flag = -1`, no `seas_12.se1`, no analytic fallback for asteroids), and one refused body used to take the whole chart down. It is now reported in an optional **`unavailableBodies`** array of `{ name, longitude: null, reason }` and the other twelve bodies are returned. Deliberately *not* F8's omit convention — the caller asked for Chiron, so silence would be indistinguishable from "not requested". Human Design and Gene Keys still throw, because a partial body set is a structurally wrong bodygraph rather than an incomplete one. (F2) `tests/ephemeris-fallback.test.ts` (17 tests) pins both directions of the label so a hardcoded one fails; write-up in `docs/accuracy.md` §6.

## Tech Debt (code quality)

- [ ] OpenAPI declares `BirthData` as the request body for **both** `/api/v1/synastry` and `/api/v1/transit/natal`, and it is wrong for both — carded `t_3176e10d`. Synastry's real input is `{personA, personB, aspects?, orbs?}`; transit/natal's is `{natal, transit_datetime, transit_timezone, transit_planets?, aspects?, orbs?}`. Neither operation documents its `orbs` or `aspects` option, so the two endpoints that have had an orb override the longest are the two the spec describes least — `/api/v1/astrology/transits` now documents `orbs` properly (`t_a05bb333`) while these two do not. Add `SynastryInput` and `TransitToNatalInput` components in `src/lib/openapi/spec.ts`, both `$ref`ing the existing `OrbOverride`. Synastry is also the only place the `rulership` option is undocumented (other endpoints get it via the `NatalInput` $ref). Found in the v0.3.0 contract audit, re-confirmed 2026-08-13.
- [ ] The existing synastry orb-override test asserts `toBeLessThanOrEqual`, which a total no-op override also satisfies — carded `t_a02c9654`. It proves the field is accepted, not that it does anything. Tighten to a strict inequality against a measured baseline, the way `tests/aspect-conventions.test.ts` §1.3 now does for `/api/v1/astrology/transits`. Found while writing that test.
- [x] OpenAPI `info.version` was hard-coded `"0.1.0"` while `package.json` was `0.3.0`, so the live docs page advertised a version that had not existed for two releases. **Fixed 2026-08-13**: both this and the health endpoint now import `API_VERSION` from `src/lib/version.ts`, the single source of truth, so a third consumer cannot drift. `tests/version.test.ts` asserts every surface against `package.json` and rejects a semver literal reappearing in any of them. Verified on a production build: both endpoints report `0.3.0`.
- [ ] Independent accuracy oracle — every position/house test is anchored to Astrodienst, which runs the same Swiss Ephemeris we do, so those are **consistency** checks, not accuracy ones. The only genuinely independent check is `tests/sun-position-meeus.test.ts` (Meeus 1998 ch.25). The Moon, all planets, the nodes, Chiron, and every house system have no external anchor; the local-time → UT → JD pipeline is the highest-risk untested path. Candidate oracle: JPL Horizons.
- [x] `tests/human-design-snapshot.test.ts` header was stale — claimed `GATE_WHEEL_OFFSET` is `3.875°` and "not yet verified", and the describe block said `PROVISIONAL`. The offset is `358.25°`, anchored to the Rave Mandala convention (Gate 41 at 2°00′ Aquarius = 302.00°) and asserted in `tests/hd-gates.test.ts`. Header rewritten 2026-08-12; the file's assertions are range/enum invariants, not captured goldens.
- [ ] Additional strict-lint pass — current setup enforces zero warnings; consider narrower ESLint overrides file-by-file if needed.

## Enhancements (nice to have)

- [ ] Davison chart endpoint (time/space-midpoint variant; midpoint composite shipped 2026-07-24).
- [ ] Client-side test coverage for new UI (Transits tab, Sky page, Synastry sub-tab, planet/aspect filter chips).
- [ ] HD PHS sub-labels — MyBodyGraph shows Digestion labels like "Buzzing (Nervous Touch)" and directional labels like "Direct"/"InDirect". Map color+tone+base sub-classifications; needs lookup tables per variable.
- [ ] Verify J (Juxtaposition) cross names against MyBodyGraph — RA and LA are structurally verified via quaternary invariants; J entries are single-source only.
- [ ] Sabian Symbols — 360-degree symbolic image per zodiac degree (Marc Edmund Jones / Elsie Wheeler). Needs the canonical 360-entry table.
- [ ] Cards of Destiny Planetary Ruling Card + Karma Cards (needs verified lookup tables)
- [ ] Topocentric flag — design concurrency for sweph's global `set_topo` state. Mainly affects Moon (~1°)
- [ ] Sidereal zodiac with selectable ayanamsa (Lahiri / Krishnamurti / Fagan-Bradley)
- [ ] Caching layer (in-memory LRU + Vercel Runtime Cache)
- [ ] Rate limiting via Vercel Firewall WAF rules
- [ ] OpenAPI response schemas — responses are currently description-only, so consumers can't codegen response types (`patterns`, `chartRuler`, `referenceLatitudeSource` documented only in prose); ideally derive from Zod output schemas. Found in v0.3.0 contract audit.
- [ ] SDK packages (`@astro-calculator/client-ts`, Python)
- [ ] Render astrocartography lines on an interactive world map in `/chart`
- [ ] Cross-validate Mandela fixture values against Astrodienst once its Cloudflare gate is passable (currently seeded from our sweph-based calculator).
